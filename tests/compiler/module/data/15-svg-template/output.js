import { component, svg } from "ivi";
import { _T, _sN, _t, EMPTY_ARRAY as _EMPTY_ARRAY } from "ivi";
import { dedupe as _dedupe } from "oveo";
const _TPL_ = (void 0, "@ivi.tpl", _T(_dedupe(_sN(`<a a></a>`)), 4097, _EMPTY_ARRAY, _EMPTY_ARRAY, _EMPTY_ARRAY));
const c = component(() => {
	return (v) => _t(_TPL_);
});
