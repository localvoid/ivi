{
  "private": true,
  "scripts": {
    "dist": "tsc -b",
    "clean": "tsc -b --clean",
    "ts:watch": "tsc -b -w --pretty",
    "ts:force": "tsc -b --force --pretty",
    "test": "node --enable-source-maps --test packages/",
    "test:only": "node --enable-source-maps --test-only --test packages/"
  },
  "devDependencies": {
    "@types/node": "^18.14.6",
    "typescript": "^4.9.5"
  }
}