{
  "private": true,
  "packageManager": "pnpm@8.5.1",
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b -w --pretty",
    "build:force": "tsc -b --force --pretty",
    "test": "node --enable-source-maps --test packages/ tests/dist/",
    "test:only": "node --enable-source-maps --test-only --test packages/ tests/dist/"
  },
  "devDependencies": {
    "@types/node": "^20.6.0",
    "typescript": "^5.2.2"
  },
  "volta": {
    "node": "20.6.0",
    "pnpm": "8.7.1"
  }
}