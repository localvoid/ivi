{
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b -w --pretty",
    "build:force": "tsc -b --force --pretty",
    "clean": "tsc -b --clean",
    "test": "node --enable-source-maps --test packages/@ivi/ packages/ivi/dist/__tests__/",
    "test:only": "node --enable-source-maps --test-only --test packages/@ivi/ packages/ivi/dist/__tests__/"
  },
  "devDependencies": {
    "@types/node": "^18.15.5",
    "global-jsdom": "^8.8.0",
    "jsdom": "^21.1.1",
    "typescript": "^5.0.2"
  },
  "volta": {
    "node": "18.15.0",
    "pnpm": "7.30.3"
  }
}