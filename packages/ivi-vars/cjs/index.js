/**
 * Development Mode.
 * 
 * @const {boolean}
 */
const DEV = true;

/**
 * Target Environment.
 * 
 * @const {number}
 */
const TARGET = 1;

module.exports.DEV = DEV;
module.exports.TARGET = TARGET;
